sudo: required
language: java
jobs:
    include:
        - env: test='precise openjdk not on image'
          jdk: openjdk9
          dist: precise
        - env: test='precise openjdk on image'
          jdk: openjdk7
          dist: precise
        - env: test='precise oraclejdk not on image'
          jdk: oraclejdk11
          dist: precise
        - env: test='precise oraclejdk on image'
          jdk: oraclejdk7
          dist: precise
install: "ls -la /usr/lib/jvm /usr/local/lib/jvm || true"
script:
    - which java
    - ls -l `which java`
    - "tr : '\n' <<<$PATH"
    - echo $JAVA_HOME
notifications:
  email: never

