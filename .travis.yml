language: java
jobs:
  include:
    - name: precise
      dist: precise
    - name: trusty
      dist: trusty
    - name: xenial
      dist: xenial
install: skip
script: sudo apt-get update
notifications:
  email: never
